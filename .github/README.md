# GitHub

This directory is the minimal GitHub-facing scaffold for `spec-of-dust`.

Intended uses:

- release notes and asset conventions
- future Pages or Actions configuration
- public-facing repo hygiene that should not clutter the root

Current publishing shape:

- source of truth lives in the repo root
- lightweight public docs can ship from `docs/`
- versioned packs and starter templates can ship as GitHub Release artifacts
