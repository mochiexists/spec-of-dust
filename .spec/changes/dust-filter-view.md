status: done
files: templates/dust.html, docs/dust.html

# Add dust filter view to show only the poetic closure lines

## What
The viewer already renders the "Dust" closure line for each completed change as a styled italic line, but it's mixed in with all the other filter views (All, Devlog, Flowlog, Changes). Add a "Dust" filter that shows *only* the dust lines from completed changes — a compact, gallery-style view where the poetic one-liners are the focus, not the metadata around them. This makes the dust itself browsable as a body of work.

## Acceptance criteria
- [ ] `templates/dust.html` adds a new filter button `<button class="filter-btn" data-filter="dust">Dust</button>` to the toolbar, positioned last after "Changes"
- [ ] Membership: Dust view includes only entries where `_source === "change"` AND `status === "done"` AND the dust field passes a shared `hasRealDust(entry)` helper (non-empty, not `nothing notable`, not just the `- Dust:` label). Devlog, flowlog, and non-done change entries are all hidden.
- [ ] A shared helper function `hasRealDust(entry)` is added at module scope and used by BOTH the filter membership check AND the existing `buildChangeEntry` dust-line rendering, so they cannot diverge
- [ ] Each shown entry renders in a minimal form: just the dust line, the change name, and the date. Closure metadata (challenges/learnings/outcomes), peer reviews, and verify sections are hidden in this view.
- [ ] The "Active Work" section at top is hidden when Dust filter is active (consistent with status=done filter)
- [ ] The "X shown" count reflects the number of visible Dust cards, not the raw `change`-sourced count
- [ ] Both `templates/dust.html` (source-of-truth for fresh setups) AND `docs/dust.html` (this repo's instance) receive the same HTML/JS changes. `build-dust.sh` only rewrites the embedded-data block, so template structural changes must also be applied to `docs/dust.html` directly. `bash scripts/build-dust.sh --check` passes after the change (data block consistent).

## Notes
- The existing inline `isReal()` check in `buildChangeEntry` guards the dust-line rendering today. Extract to module-scope `hasRealDust(entry)` so filter membership and rendering share one source of truth.
- No new CSS needed — the `.dust-line` class already has the styling. We just omit the other fields when rendering in dust mode.
- This is UI-only: `templates/dust.html` for source-of-truth, `docs/dust.html` auto-regenerated via `scripts/build-dust.sh`. No script logic changes, no gate changes, no b-startup changes.
- Keep it simple: no sorting controls, no search. Just the dust, chronologically (same order as the main view).

## Peer spec review
**Codex** (2026-04-17, gpt-5.4):

1. Blocker: fuzzy "completed/archived" membership. → Fixed: membership now precise — `_source === "change"` AND `status === "done"` AND `hasRealDust(entry)`.
2. Blocker: "regenerated" was too loose. → Fixed: AC names `bash scripts/build-dust.sh` and the `--check` validation.
3. Risk: isReal is inline-scoped. → Fixed: AC requires extracting `hasRealDust(entry)` to module scope for shared use.
4. Advisory: "gallery-style" overbuilt language. → Fixed: dropped.
5. Advisory: count must match visible cards. → Fixed: AC now explicit.


## Peer code review
**Codex** (2026-04-17, gpt-5.4):

1. Blocker: spec said docs/dust.html is "regenerated by build-dust.sh" but build-dust.sh only rewrites the data block. → Fixed: AC now clarifies that template structural changes must be applied to both files (build-dust.sh full regen is a future change).
2. Medium: count regressed for `all`/`change` filters — active work section no longer counted. → Fixed: count now sums timelineEntries + activeChanges when active section is shown.
3. Medium: Dust cards still showed "change" source badge. → Fixed: removed badge from `buildDustEntry`; shows only ts, name, dust line.
4. Advisory: `hasRealDust` accepted whitespace-only. → Fixed: now trims before comparing.


## Verify
- [pass] Filter button added after "Changes" — confirmed in both `templates/dust.html` and `docs/dust.html` toolbar
- [pass] Membership: filter shows only `_source === "change"` AND `status === "done"` AND `hasRealDust(entry)` — verified via simulation with test data (excludes "nothing notable", empty, non-done)
- [pass] Shared `hasRealDust(entry)` helper at module scope, used by filter AND `buildChangeEntry` dust-line render
- [pass] Dust cards show only ts, name, dust line (no source badge, no closure fields, no peer review)
- [pass] "Active Work" section hidden when Dust filter active (status=done filter excludes active)
- [pass] Count reflects visible cards — timelineEntries + activeChanges when active shown, otherwise timelineEntries
- [pass] Both `templates/dust.html` and `docs/dust.html` updated with identical structural changes; `build-dust.sh --check` passes


## Closure
- Challenges: surfaced an architectural gap — build-dust.sh only updates data, not HTML structure, so template edits need manual mirroring to docs/dust.html
- Learnings: template and docs are loosely coupled by design; a future change could make build-dust.sh a full regenerator
- Outcomes: Dust view lets you browse the poetic body of work on its own
- Dust: the dust finds its own shelf
