# Publish dust on mochiexists

Date gathered: 2026-04-17

## Recommendation

Publish `spec-of-dust` publicly as `dust` on `mochiexists.com/dust`.

Keep the internal/repo name as `spec-of-dust`. Use `dust` only as the public
label and page slug.

## Homepage copy

The live Mochi homepage currently lists:

- `clawpost`
- `plate`
- `story`
- `yolo`

Recommendation: change the homepage label from `clawpost` to `post`.

That keeps the homepage copy shorter and cleaner if the slot is acting as a
project/category label instead of repeating the full product brand.

## Evidence trail

Public evidence collected from the live site:

1. `https://mochiexists.com` currently exposes the `yolo` page.
2. `https://mochiexists.com/yolo` links to `https://github.com/mochiexists/yolo`.
3. That establishes `mochiexists` as the public GitHub owner behind at least one
   live Mochi page.

## Current limitation

The local source for the Mochi homepage was not identified in the current
workspace during this pass, so this note records the public naming and routing
recommendation, not an implementation path inside the site repo.
